{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "crons": [
    { "path": "/api/cron/expire-reservations", "schedule": "*/5 * * * *" },
    { "path": "/api/cron/reconcile-stripe", "schedule": "*/15 * * * *" }
  ]
}
