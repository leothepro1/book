/**
 * Config Card Bootstrap
 *
 * Import this file to register all section config cards.
 * Each card self-registers via registerSectionConfig() at import time.
 *
 * Add new config card imports here as they are created.
 */

import "./hero-slider";
import "./category-tabs";
