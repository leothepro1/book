import { registerCheckinCard } from "../registry";

registerCheckinCard({
  id: "signature",
  label: "Signatur",
  icon: "draw",
  version: "1.0.0",
  optional: true,
  defaultEnabled: true,
  defaultSortOrder: 0,
});
